<%# in app/views/books/new.html.erb %>
<%= form_with model: @book, local: false, format: :js, id: "bookcreate" do |book_form| %>

  <%=render 'form_template', book_form: book_form %>

  <br>
  <%= book_form.submit "Create a new book" %>
<% end %>
<%# Can also use min and max in number fields. %>
